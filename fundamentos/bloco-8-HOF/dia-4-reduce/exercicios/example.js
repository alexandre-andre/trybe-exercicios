const funcaoDoReducer = (acumulador, valorAtual) => {
    // colocar coisas dentro de um array -> variavel.push
    // const obj = {}
    // acumulador.push(objeto)
    // return acumulador
    // find retorna o objeto que satisfaça a condição, daí no final você bota .o que você quer acessar
  }
  // faz esse primeiro
  
  // como fazer um array de objetos com reduce:
  const array = pedidos.reduce(funcaoDoReducer, []) // faz esse primeiro
  //-----------
  // o acumulador guarda os resultados e o elementoAtual dá elementos pra você trabalhar